# Chave Secreta do Flask (Obrigatória para sessões)
# Gere uma chave forte (ex: python -c 'import secrets; print(secrets.token_hex(24))')
SECRET_KEY=

# Configurações do Google Cloud (do PRD)
GOOGLE_CLOUD_PROJECT=

# Configurações do Flask (opcional para desenvolvimento)
FLASK_DEBUG=True
